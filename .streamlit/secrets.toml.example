# Copy to secrets.toml for local testing (secrets.toml is gitignored).
# On Streamlit Cloud: App settings → Secrets → paste the same block.

COLLAR_PASSWORD_HASH = "pbkdf2_sha256$PASTE_SALT_AND_HASH_FROM_hash_collar_password.py"
